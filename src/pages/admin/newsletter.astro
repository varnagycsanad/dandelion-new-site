---
import BaseLayout from "../../layouts/BaseLayout.astro";
import NewsletterSidebar from "../../components/admin/NewsletterSidebar.astro";

const newsletterApiUrl =
  import.meta.env.PUBLIC_NEWSLETTER_API_URL ??
  import.meta.env.NEWSLETTER_PUBLIC_BASE_URL ??
  "/newsletter-api";
const pageTitle = "Áttekintés";
const pageDescription = "Hírlevél admin dashboard a feliratkozók, listák, csoportok és kampányok gyors áttekintéséhez.";
---

<BaseLayout title={pageTitle} description={pageDescription} hideHeader={true} hideFooter={true} noindex={true}>
  <section class="admin-newsletter" data-admin-newsletter data-api-base={newsletterApiUrl}>
    <div class="admin-newsletter__backdrop admin-newsletter__backdrop--gold"></div>
    <div class="admin-newsletter__backdrop admin-newsletter__backdrop--sage"></div>

    <div class="admin-newsletter__gate" data-gate>
      <div class="admin-newsletter__gate-card">
        <div class="admin-newsletter__gate-head">
          <div>
            <p class="admin-newsletter__eyebrow">Newsletter hub</p>
            <strong class="admin-newsletter__gate-title">Kapcsolat és belépés</strong>
          </div>
          <div class="admin-newsletter__connection-flag" data-connection-flag aria-live="polite">
            Ellenőrzés indul...
          </div>
        </div>

        <div class="admin-newsletter__diagnostics" aria-label="Ellenőrzési állapot">
          <div class="admin-newsletter__diagnostic-row">
            <div class="admin-newsletter__diagnostic">
              <span>API</span>
              <strong data-diagnostic-step="health">Várakozás</strong>
            </div>
            <div class="admin-newsletter__diagnostic">
              <span>Admin</span>
              <strong data-diagnostic-step="auth">Várakozás</strong>
            </div>
            <div class="admin-newsletter__diagnostic">
              <span>Adat</span>
              <strong data-diagnostic-step="data">Várakozás</strong>
            </div>
          </div>
        </div>

        <div class="admin-newsletter__gate-actions">
          <button class="admin-newsletter__button admin-newsletter__button--ghost" type="button" data-check-connection>
            Újraellenőrzés
          </button>
          <a class="admin-newsletter__button" href="/admin/">Vissza az adminhoz</a>
        </div>

        <p class="admin-newsletter__gate-status" data-gate-status aria-live="polite">
          Belépés és kapcsolat ellenőrzése folyamatban...
        </p>
      </div>
    </div>

    <div class="admin-newsletter__shell" hidden>
      <NewsletterSidebar activeSection="dashboard" />

      <div class="admin-newsletter__content">
        <header class="admin-newsletter__header">
          <div class="admin-newsletter__header-copy">
            <p class="admin-newsletter__eyebrow">Dandelion admin</p>
            <h1>{pageTitle}</h1>
            <p class="admin-newsletter__lead">
              Itt látod a hírlevél rendszer alapadatait. A dashboard a feliratkozók számát, a listák megoszlását,
              a csoportokat és a kampányokat mutatja, hogy azonnal tudd, mi működik.
            </p>
          </div>

          <div class="admin-newsletter__header-actions">
            <a class="admin-newsletter__button admin-newsletter__button--ghost" href="/admin/hirlevel/">
              Feliratkozók
            </a>
            <a class="admin-newsletter__button admin-newsletter__button--ghost" href="/admin/kampanyok/">
              Kampányok
            </a>
            <button class="admin-newsletter__button" type="button" data-refresh-dashboard>
              Adatok frissítése
            </button>
          </div>
        </header>

        <section class="admin-newsletter__summary" aria-label="Fő statisztikák">
          <article class="admin-newsletter__metric">
            <span>Összes feliratkozó</span>
            <strong data-dashboard-stat="subscribers">0</strong>
            <p>Az összes betöltött cím a service-ből.</p>
          </article>

          <article class="admin-newsletter__metric">
            <span>Aktív címek</span>
            <strong data-dashboard-stat="active">0</strong>
            <p>Kiknek lehet most levelet küldeni.</p>
          </article>

          <article class="admin-newsletter__metric">
            <span>Függőben</span>
            <strong data-dashboard-stat="pending">0</strong>
            <p>Folyamatban lévő feliratkozások.</p>
          </article>

          <article class="admin-newsletter__metric">
            <span>Leiratkozott</span>
            <strong data-dashboard-stat="unsubscribed">0</strong>
            <p>Jelenleg kizárt címek.</p>
          </article>

          <article class="admin-newsletter__metric">
            <span>Listák</span>
            <strong data-dashboard-stat="lists">0</strong>
            <p>Hírlevél, korábbi vendégek és egyéb listák.</p>
          </article>

          <article class="admin-newsletter__metric">
            <span>Csoportok</span>
            <strong data-dashboard-stat="groups">0</strong>
            <p>Későbbi kampánycélzásokhoz.</p>
          </article>

          <article class="admin-newsletter__metric">
            <span>Kampányok</span>
            <strong data-dashboard-stat="campaigns">0</strong>
            <p>Összes nyilvántartott kampány.</p>
          </article>

          <article class="admin-newsletter__metric">
            <span>Küldések</span>
            <strong data-dashboard-stat="deliveries">0</strong>
            <p>Naplózott levelezési események.</p>
          </article>
        </section>

        <section class="admin-newsletter__panel">
          <div class="admin-newsletter__panel-head">
            <div>
              <p class="admin-newsletter__eyebrow">Listák</p>
              <h2>Lista bontás</h2>
            </div>
            <p class="admin-newsletter__panel-note">
              Itt látszik, hogy a különböző listákon hány aktív, függőben lévő vagy leiratkozott cím van.
            </p>
          </div>

          <div class="admin-newsletter__list-grid" data-list-grid aria-live="polite">
            <div class="admin-newsletter__empty-state">
              <strong>Nincs listaadat</strong>
              <p>
                A lista-bontás belépés után töltődik be a newsletter service-ből.
              </p>
            </div>
          </div>
        </section>

        <section class="admin-newsletter__panel admin-newsletter__panel--compact">
          <div class="admin-newsletter__panel-head">
            <div>
              <p class="admin-newsletter__eyebrow">Gyors műveletek</p>
              <h2>Mit érdemes innen csinálni?</h2>
            </div>
          </div>

          <div class="admin-newsletter__action-grid">
            <a class="admin-newsletter__action-card" href="/admin/hirlevel/">
              <strong>Feliratkozók kezelése</strong>
              <span>Keresés, szűrés, import és manuális felvitel.</span>
            </a>
            <a class="admin-newsletter__action-card" href="/admin/kampanyok/">
              <strong>Kampányok indítása</strong>
              <span>Draft, teszt, célcsoport és éles kiküldés.</span>
            </a>
          </div>
        </section>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  import {
    buildAdminFetch,
    clearStoredAdminAuth,
    getStoredAdminSessionToken,
  } from "../../scripts/newsletterAdminAuth";

  (() => {
    const root = document.querySelector("[data-admin-newsletter]");
    if (!(root instanceof HTMLElement)) return;

    const apiBase = root.dataset.apiBase?.trim() || "";
    const gate = root.querySelector("[data-gate]");
    const shell = root.querySelector(".admin-newsletter__shell");
    const gateStatus = root.querySelector("[data-gate-status]");
    const connectionFlag = root.querySelector("[data-connection-flag]");
    const diagnostics = {
      health: root.querySelector('[data-diagnostic-step="health"]'),
      auth: root.querySelector('[data-diagnostic-step="auth"]'),
      data: root.querySelector('[data-diagnostic-step="data"]'),
    };
    const checkButton = root.querySelector("[data-check-connection]");
    const refreshButton = root.querySelector("[data-refresh-dashboard]");
    const listGrid = root.querySelector("[data-list-grid]");
    const statNodes = {
      subscribers: root.querySelector('[data-dashboard-stat="subscribers"]'),
      active: root.querySelector('[data-dashboard-stat="active"]'),
      pending: root.querySelector('[data-dashboard-stat="pending"]'),
      unsubscribed: root.querySelector('[data-dashboard-stat="unsubscribed"]'),
      lists: root.querySelector('[data-dashboard-stat="lists"]'),
      groups: root.querySelector('[data-dashboard-stat="groups"]'),
      campaigns: root.querySelector('[data-dashboard-stat="campaigns"]'),
      deliveries: root.querySelector('[data-dashboard-stat="deliveries"]'),
    };

    const formatCount = (value) => Number(value ?? 0).toLocaleString("hu-HU");

    const setTone = (node, tone, text) => {
      if (!(node instanceof HTMLElement)) return;
      node.dataset.tone = tone;
      node.textContent = text;
    };

    const setGateStatus = (message, tone = "info") => {
      setTone(gateStatus, tone, message);
    };

    const setConnectionFlag = (message, tone = "info") => {
      setTone(connectionFlag, tone, message);
    };

    const setStep = (step, message, tone = "info") => {
      setTone(diagnostics[step], tone, message);
    };

    const setLocked = (locked) => {
      if (gate instanceof HTMLElement) {
        gate.hidden = !locked;
      }
      if (shell instanceof HTMLElement) {
        shell.hidden = locked;
      }
    };

    const renderLists = (lists) => {
      if (!(listGrid instanceof HTMLElement)) return;

      if (!Array.isArray(lists) || !lists.length) {
        listGrid.innerHTML = `
          <div class="admin-newsletter__empty-state">
            <strong>Nincs listaadat</strong>
            <p>A lista-bontás csak akkor jelenik meg, ha a newsletter service eléri a listák részleteit.</p>
          </div>
        `;
        return;
      }

      listGrid.innerHTML = lists
        .map(
          (item) => `
            <article class="admin-newsletter__list-card">
              <div class="admin-newsletter__list-card-head">
                <div>
                  <p class="admin-newsletter__list-key">${String(item?.key ?? "").replaceAll("-", " ")}</p>
                  <h3>${String(item?.name ?? item?.key ?? "Ismeretlen lista")}</h3>
                </div>
                <strong>${formatCount(item?.total ?? 0)}</strong>
              </div>
              ${String(item?.description ?? "").trim() ? `<p class="admin-newsletter__list-description">${String(item.description).trim()}</p>` : ""}
              <div class="admin-newsletter__list-stats">
                <span><strong>${formatCount(item?.active ?? 0)}</strong>aktív</span>
                <span><strong>${formatCount(item?.pending ?? 0)}</strong>függőben</span>
                <span><strong>${formatCount(item?.unsubscribed ?? 0)}</strong>leiratkozott</span>
              </div>
            </article>
          `
        )
        .join("");
    };

    const updateStats = (counts, lists) => {
      const nextCounts = {
        subscribers: Number(counts?.subscribers ?? 0),
        active: Array.isArray(lists) ? lists.reduce((sum, item) => sum + Number(item?.active ?? 0), 0) : 0,
        pending: Array.isArray(lists) ? lists.reduce((sum, item) => sum + Number(item?.pending ?? 0), 0) : 0,
        unsubscribed: Array.isArray(lists) ? lists.reduce((sum, item) => sum + Number(item?.unsubscribed ?? 0), 0) : 0,
        lists: Number(counts?.lists ?? 0),
        groups: Number(counts?.groups ?? 0),
        campaigns: Number(counts?.campaigns ?? 0),
        deliveries: Number(counts?.deliveries ?? 0),
      };

      Object.entries(statNodes).forEach(([key, node]) => {
        if (node instanceof HTMLElement) {
          node.textContent = formatCount(nextCounts[key]);
        }
      });
    };

    const loadDashboard = async () => {
      const sessionToken = getStoredAdminSessionToken();
      if (!sessionToken) {
        window.location.replace("/admin/");
        return;
      }

      if (!apiBase) {
        setLocked(true);
        setConnectionFlag("API hiányzik", "error");
        setStep("health", "Nincs cím", "error");
        setStep("auth", "Nem ellenőrizhető", "warning");
        setStep("data", "Nem tölthető", "warning");
        setGateStatus("A newsletter API címe nincs beállítva.", "error");
        return;
      }

      setLocked(true);
      setConnectionFlag("Ellenőrzés...", "info");
      setStep("health", "Kapcsolódás...", "info");
      setStep("auth", "Session ellenőrzés...", "info");
      setStep("data", "Adatok várnak", "info");
      setGateStatus("Belépés és kapcsolat ellenőrzése folyamatban...", "info");

      try {
        const authResponse = await buildAdminFetch(apiBase, "/admin/auth", {
          method: "POST",
          headers: { Accept: "application/json" },
          body: "{}",
        });
        const authResult = await authResponse.json().catch(() => ({}));

        if (!authResponse.ok || !authResult?.ok) {
          throw new Error(authResult?.message || "auth_failed");
        }

        setStep("auth", "Belépve", "success");

        const [healthResponse, listsResponse] = await Promise.all([
          buildAdminFetch(apiBase, "/health", {
            headers: { Accept: "application/json" },
          }),
          buildAdminFetch(apiBase, "/lists", {
            headers: { Accept: "application/json" },
          }),
        ]);

        const healthResult = await healthResponse.json().catch(() => ({}));
        const listsResult = await listsResponse.json().catch(() => ({}));

        if (!healthResponse.ok || !healthResult?.ok) {
          throw new Error("health_failed");
        }
        setStep("health", "API elérhető", "success");

        if (!listsResponse.ok || !listsResult?.ok) {
          throw new Error(listsResult?.message || "lists_failed");
        }
        setStep("data", "Adatok betöltve", "success");

        updateStats(healthResult.counts ?? {}, listsResult.lists ?? []);
        renderLists(listsResult.lists ?? []);
        setConnectionFlag("Admin aktív", "success");
        setGateStatus("A newsletter dashboard biztonságosan betöltve.", "success");
        setLocked(false);
      } catch (error) {
        console.error("[admin-newsletter-dashboard]", error);
        clearStoredAdminAuth();
        setLocked(true);
        setConnectionFlag("Hiba", "error");
        setStep("auth", "Belépés lejárt", "error");
        setStep("health", "API hiba", "warning");
        setStep("data", "Nincs adat", "warning");
        setGateStatus("A dashboard csak érvényes admin sessionnel érhető el. Jelentkezz be újra.", "error");
        window.setTimeout(() => {
          window.location.replace("/admin/");
        }, 1200);
      }
    };

    checkButton?.addEventListener("click", loadDashboard);
    refreshButton?.addEventListener("click", loadDashboard);

    loadDashboard();
  })();
</script>

<style>
  .admin-newsletter {
    position: relative;
    min-height: 100vh;
    padding: 28px 0 52px;
    isolation: isolate;
    background:
      radial-gradient(circle at top left, rgba(212, 173, 89, 0.16), transparent 25%),
      radial-gradient(circle at 85% 10%, rgba(104, 137, 108, 0.12), transparent 24%),
      linear-gradient(180deg, #f8f2e6 0%, #f4efdf 100%);
  }

  .admin-newsletter__backdrop {
    position: absolute;
    border-radius: 999px;
    filter: blur(18px);
    opacity: 0.5;
    pointer-events: none;
  }

  .admin-newsletter__backdrop--gold {
    top: 20px;
    right: -120px;
    width: 280px;
    height: 280px;
    background: rgba(203, 166, 93, 0.35);
  }

  .admin-newsletter__backdrop--sage {
    top: 220px;
    left: -100px;
    width: 240px;
    height: 240px;
    background: rgba(126, 150, 116, 0.24);
  }

  .admin-newsletter__gate {
    position: relative;
    z-index: 5;
    width: min(1120px, calc(100% - 32px));
    margin: 0 auto 18px;
  }

  .admin-newsletter__gate[hidden] {
    display: none;
  }

  .admin-newsletter__gate-card {
    width: 100%;
    padding: 18px 22px;
    border-radius: 22px;
    border: 1px solid rgba(92, 67, 37, 0.12);
    background: rgba(255, 252, 247, 0.9);
    box-shadow: 0 10px 26px rgba(79, 60, 34, 0.08);
    display: grid;
    gap: 14px;
  }

  .admin-newsletter__gate-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
  }

  .admin-newsletter__gate-title {
    display: block;
    margin-top: 2px;
    color: #241c16;
    font-size: 1rem;
    font-weight: 600;
  }

  .admin-newsletter__eyebrow {
    margin: 0;
    color: #a57d37;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  .admin-newsletter__connection-flag {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    min-height: 32px;
    padding: 0 12px;
    border-radius: 999px;
    border: 1px solid rgba(92, 67, 37, 0.12);
    background: rgba(255, 252, 247, 0.96);
    color: #5b4a39;
    font-size: 0.88rem;
    font-weight: 600;
    line-height: 1;
  }

  .admin-newsletter__connection-flag[data-tone="success"] {
    border-color: rgba(57, 123, 73, 0.18);
    background: rgba(231, 247, 232, 0.96);
    color: #1d6b37;
  }

  .admin-newsletter__connection-flag[data-tone="warning"] {
    border-color: rgba(201, 156, 59, 0.2);
    background: rgba(255, 245, 224, 0.96);
    color: #8a5a00;
  }

  .admin-newsletter__connection-flag[data-tone="error"] {
    border-color: rgba(196, 94, 94, 0.2);
    background: rgba(255, 236, 236, 0.96);
    color: #9b2c2c;
  }

  .admin-newsletter__diagnostics {
    display: grid;
    gap: 10px;
  }

  .admin-newsletter__diagnostic-row {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
  }

  .admin-newsletter__diagnostic {
    display: grid;
    gap: 6px;
    padding: 12px 14px;
    border-radius: 16px;
    border: 1px solid rgba(92, 67, 37, 0.1);
    background: rgba(255, 255, 255, 0.76);
  }

  .admin-newsletter__diagnostic span {
    color: rgba(102, 78, 42, 0.78);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .admin-newsletter__diagnostic strong {
    font-size: 0.94rem;
    line-height: 1.35;
    font-weight: 600;
  }

  .admin-newsletter__diagnostic strong[data-tone="success"] {
    color: #1d6b37;
  }

  .admin-newsletter__diagnostic strong[data-tone="warning"] {
    color: #8a5a00;
  }

  .admin-newsletter__diagnostic strong[data-tone="error"] {
    color: #9b2c2c;
  }

  .admin-newsletter__diagnostic strong[data-tone="info"] {
    color: #5b4a39;
  }

  .admin-newsletter__gate-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  .admin-newsletter__gate-status {
    margin: 0;
    padding: 11px 14px;
    border-radius: 14px;
    background: rgba(255, 255, 255, 0.82);
    color: #4c3d2e;
    border: 1px solid rgba(92, 67, 37, 0.1);
    line-height: 1.5;
    font-size: 0.95rem;
  }

  .admin-newsletter__gate-status[data-tone="success"] {
    color: #1d6b37;
    border-color: rgba(57, 123, 73, 0.18);
    background: rgba(231, 247, 232, 0.94);
  }

  .admin-newsletter__gate-status[data-tone="warning"] {
    color: #8a5a00;
    border-color: rgba(201, 156, 59, 0.2);
    background: rgba(255, 245, 224, 0.96);
  }

  .admin-newsletter__gate-status[data-tone="error"] {
    color: #9b2c2c;
    border-color: rgba(196, 94, 94, 0.2);
    background: rgba(255, 236, 236, 0.96);
  }

  .admin-newsletter__shell {
    position: relative;
    z-index: 1;
    width: 100%;
    margin: 0;
    padding-right: 18px;
    box-sizing: border-box;
    display: grid;
    grid-template-columns: minmax(260px, 300px) minmax(0, 1fr);
    gap: 22px;
    align-items: start;
  }

  .admin-newsletter__content {
    min-width: 0;
    display: grid;
    gap: 18px;
  }

  .admin-newsletter__header {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 22px;
  }

  .admin-newsletter__header-copy h1 {
    margin: 8px 0 0;
    font-family: var(--dnd-font-display);
    font-size: clamp(2.2rem, 4vw, 3.7rem);
    line-height: 1;
    color: #241c16;
    font-weight: 600;
  }

  .admin-newsletter__lead {
    max-width: 820px;
    margin: 14px 0 0;
    color: #5b4a39;
    font-size: 1rem;
    line-height: 1.6;
  }

  .admin-newsletter__header-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 10px;
    flex-shrink: 0;
  }

  .admin-newsletter__button {
    min-height: 42px;
    padding: 0 16px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    text-decoration: none;
    color: #fff9ef;
    background: linear-gradient(135deg, #272218 0%, #6c5130 100%);
    box-shadow: 0 14px 22px rgba(63, 47, 26, 0.16);
    font-size: 0.96rem;
    font-weight: 600;
    border-radius: 18px;
    cursor: pointer;
  }

  .admin-newsletter__button--ghost {
    background: rgba(255, 252, 247, 0.9);
    color: #3f3124;
    box-shadow: inset 0 0 0 1px rgba(92, 67, 37, 0.1);
  }

  .admin-newsletter__summary {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 14px;
  }

  .admin-newsletter__metric {
    display: grid;
    gap: 10px;
    padding: 18px;
    border-radius: 22px;
    background: rgba(255, 252, 247, 0.92);
    box-shadow:
      inset 0 0 0 1px rgba(92, 67, 37, 0.08),
      0 16px 40px rgba(79, 60, 34, 0.06);
  }

  .admin-newsletter__metric span {
    color: rgba(122, 95, 52, 0.86);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .admin-newsletter__metric strong {
    color: #241c16;
    font-size: clamp(1.7rem, 2.5vw, 2.3rem);
    line-height: 1;
    font-weight: 600;
  }

  .admin-newsletter__metric p {
    margin: 0;
    color: rgba(69, 55, 38, 0.82);
    font-size: 0.92rem;
    line-height: 1.45;
  }

  .admin-newsletter__panel {
    padding: 18px;
    border-radius: 28px;
    background: rgba(255, 252, 247, 0.9);
    box-shadow:
      inset 0 0 0 1px rgba(92, 67, 37, 0.08),
      0 16px 40px rgba(79, 60, 34, 0.06);
  }

  .admin-newsletter__panel--compact {
    padding-bottom: 22px;
  }

  .admin-newsletter__panel-head {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    gap: 18px;
    margin-bottom: 16px;
  }

  .admin-newsletter__panel-head h2 {
    margin: 8px 0 0;
    color: #241c16;
    font-family: var(--dnd-font-display);
    font-size: clamp(1.5rem, 2vw, 2rem);
    line-height: 1.1;
    font-weight: 600;
  }

  .admin-newsletter__panel-note {
    max-width: 520px;
    margin: 0;
    color: rgba(69, 55, 38, 0.72);
    font-size: 0.94rem;
    line-height: 1.5;
  }

  .admin-newsletter__list-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
  }

  .admin-newsletter__list-card {
    display: grid;
    gap: 12px;
    padding: 16px;
    border-radius: 22px;
    background: rgba(255, 255, 255, 0.75);
    border: 1px solid rgba(92, 67, 37, 0.08);
  }

  .admin-newsletter__list-card-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 12px;
  }

  .admin-newsletter__list-key {
    margin: 0 0 4px;
    color: rgba(122, 95, 52, 0.78);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .admin-newsletter__list-card h3 {
    margin: 0;
    color: #241c16;
    font-size: 1.05rem;
    line-height: 1.35;
    font-weight: 600;
  }

  .admin-newsletter__list-card > strong {
    color: #241c16;
    font-size: 1.5rem;
    line-height: 1;
    font-weight: 600;
  }

  .admin-newsletter__list-description {
    margin: 0;
    color: rgba(69, 55, 38, 0.78);
    font-size: 0.92rem;
    line-height: 1.5;
  }

  .admin-newsletter__list-stats {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .admin-newsletter__list-stats span {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 7px 10px;
    border-radius: 999px;
    background: rgba(245, 239, 228, 0.95);
    color: #5b4a39;
    font-size: 0.84rem;
    font-weight: 500;
  }

  .admin-newsletter__list-stats strong {
    color: #241c16;
    font-weight: 600;
  }

  .admin-newsletter__action-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
  }

  .admin-newsletter__action-card {
    display: grid;
    gap: 8px;
    padding: 16px;
    border-radius: 20px;
    background: rgba(255, 255, 255, 0.76);
    border: 1px solid rgba(92, 67, 37, 0.08);
    color: #2b241c;
    text-decoration: none;
    transition:
      transform 160ms ease,
      box-shadow 160ms ease;
  }

  .admin-newsletter__action-card strong {
    color: #241c16;
    font-size: 1rem;
    font-weight: 600;
  }

  .admin-newsletter__action-card span {
    color: rgba(69, 55, 38, 0.78);
    font-size: 0.92rem;
    line-height: 1.5;
  }

  .admin-newsletter__action-card:hover {
    transform: translateY(-2px);
    box-shadow: 0 18px 34px rgba(79, 60, 34, 0.08);
  }

  .admin-newsletter__empty-state {
    grid-column: 1 / -1;
    padding: 18px;
    border-radius: 18px;
    border: 1px dashed rgba(92, 67, 37, 0.18);
    color: #5b4a39;
    background: rgba(255, 255, 255, 0.5);
  }

  .admin-newsletter__empty-state strong {
    display: block;
    margin-bottom: 6px;
    color: #241c16;
    font-size: 0.98rem;
    font-weight: 600;
  }

  .admin-newsletter__empty-state p {
    margin: 0;
    font-size: 0.92rem;
    line-height: 1.5;
  }

  @media (max-width: 1120px) {
    .admin-newsletter__summary {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .admin-newsletter__list-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 980px) {
    .admin-newsletter__shell {
      grid-template-columns: 1fr;
      padding-right: 0;
    }

    .admin-newsletter__header,
    .admin-newsletter__panel-head {
      align-items: flex-start;
      flex-direction: column;
    }

    .admin-newsletter__header-actions {
      justify-content: flex-start;
    }

    .admin-newsletter__summary,
    .admin-newsletter__action-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 780px) {
    .admin-newsletter {
      padding: 18px 12px 28px;
    }

    .admin-newsletter__gate,
    .admin-newsletter__shell {
      width: 100%;
    }

    .admin-newsletter__gate-card,
    .admin-newsletter__panel {
      border-radius: 22px;
    }

    .admin-newsletter__diagnostic-row {
      grid-template-columns: 1fr;
    }
  }
</style>
