<section class="dnd-section dnd-newsletter-signup" aria-labelledby="newsletter-signup-title">
  <div class="dnd-container">
    <div id="mlb2-35065463" class="dnd-newsletter-signup__shell ml-subscribe-form ml-subscribe-form-35065463">
      <div class="dnd-newsletter-signup__copy row-form">
        <div class="dnd-newsletter-signup__intro">
          <span class="dnd-newsletter-signup__kicker">Dandelion hírlevél</span>
          <h2 id="newsletter-signup-title">Különleges ajánlatok és felszabaduló dátumok e-mailben.</h2>
          <p class="dnd-newsletter-signup__lead">
            Diszkrét értesítések szezonális ajánlatokról és szabad időpontokról.
          </p>
        </div>

        <form
          class="ml-block-form dnd-newsletter-signup__form"
          action="https://assets.mailerlite.com/jsonp/2003610/forms/174939941150655974/subscribe"
          method="post"
          target="_blank"
        >
          <div class="dnd-newsletter-signup__form-row">
            <div class="dnd-newsletter-signup__field ml-field-group ml-field-email ml-validate-email ml-validate-required">
              <input
                id="newsletter-email"
                aria-label="email"
                aria-required="true"
                type="email"
                name="fields[email]"
                placeholder="E-mail címed"
                autocomplete="email"
                required
              />
            </div>

            <div class="dnd-newsletter-signup__actions ml-form-embedSubmit">
              <button type="submit" class="dnd-btn dnd-btn--primary">Feliratkozás</button>
              <button disabled="disabled" type="button" class="loading dnd-newsletter-signup__loading">
                Folyamatban...
              </button>
            </div>
          </div>

          <div class="dnd-newsletter-signup__consent ml-form-checkboxRow ml-validate-required">
            <label class="dnd-newsletter-signup__checkbox">
              <input type="checkbox" required />
              <span>Hozzájárulok, hogy e-mailben értesítést kapjak a Dandelion ajánlatairól.</span>
            </label>
          </div>

          <input type="hidden" name="ml-submit" value="1" />
          <input type="hidden" name="anticsrf" value="true" />
        </form>
      </div>

      <div class="dnd-newsletter-signup__success row-success" style="display: none;">
        <span class="dnd-newsletter-signup__kicker">Sikeres feliratkozás</span>
        <h2>Köszönjük, felírtunk a listára.</h2>
        <p>Ha érkezik egy jó ajánlat vagy felszabadul egy dátum, időben szólunk.</p>
      </div>
    </div>
  </div>
</section>

<script is:inline>
  function ml_webform_success_35065463() {
    const root = document.querySelector(".ml-subscribe-form-35065463");
    if (!root) {
      return;
    }

    const formRow = root.querySelector(".row-form");
    const successRow = root.querySelector(".row-success");

    formRow?.setAttribute("style", "display: none;");
    successRow?.setAttribute("style", "display: grid;");
  }
</script>
<script src="https://groot.mailerlite.com/js/w/webforms.min.js?v176e10baa5e7ed80d35ae235be3d5024"></script>
<script is:inline>
  fetch("https://assets.mailerlite.com/jsonp/2003610/forms/174939941150655974/takel");
</script>

<style>
  .dnd-newsletter-signup {
    padding: 18px 0 26px;
    background: #fbf8f1;
  }

  .dnd-newsletter-signup__shell {
    padding: 18px 22px 16px;
    border: 1px solid rgba(91, 70, 42, 0.12);
    border-radius: 22px;
    background:
      linear-gradient(135deg, rgba(255, 254, 250, 0.98), rgba(250, 244, 233, 0.94));
    box-shadow: 0 16px 30px rgba(80, 57, 31, 0.06);
  }

  .dnd-newsletter-signup__copy,
  .dnd-newsletter-signup__success {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }

  .dnd-newsletter-signup__intro {
    display: grid;
    gap: 4px;
    flex: 1 1 auto;
    min-width: 0;
  }

  .dnd-newsletter-signup__kicker {
    display: inline-block;
    color: rgba(121, 93, 48, 0.82);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .dnd-newsletter-signup h2 {
    margin: 0;
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(1.05rem, 1.5vw, 1.35rem);
    font-weight: 400;
    line-height: 1.06;
    color: rgba(39, 32, 24, 0.94);
  }

  .dnd-newsletter-signup__lead,
  .dnd-newsletter-signup__success p {
    margin: 0;
    color: rgba(39, 32, 24, 0.72);
    font-size: 0.84rem;
    line-height: 1.35;
  }

  .dnd-newsletter-signup__form {
    display: grid;
    gap: 8px;
    flex: 0 0 auto;
    min-width: 0;
  }

  .dnd-newsletter-signup__form-row {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .dnd-newsletter-signup__field {
    width: 220px;
    min-width: 220px;
  }

  .dnd-newsletter-signup__field input {
    width: 100%;
    min-height: 42px;
    padding: 0 14px;
    border: 1px solid rgba(91, 70, 42, 0.18);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.96);
    color: rgba(39, 32, 24, 0.92);
    font: inherit;
    box-sizing: border-box;
    transition: border-color 0.18s ease, box-shadow 0.18s ease, background-color 0.18s ease;
  }

  .dnd-newsletter-signup__field input:focus {
    outline: none;
    border-color: rgba(121, 93, 48, 0.4);
    box-shadow: 0 0 0 4px rgba(255, 187, 32, 0.14);
    background: #fff;
  }

  .dnd-newsletter-signup__field input::placeholder {
    color: rgba(39, 32, 24, 0.4);
  }

  .dnd-newsletter-signup__checkbox {
    display: inline-grid;
    grid-template-columns: auto 1fr;
    gap: 8px;
    align-items: start;
    color: rgba(39, 32, 24, 0.74);
    font-size: 0.74rem;
    line-height: 1.25;
    cursor: pointer;
  }

  .dnd-newsletter-signup__checkbox input {
    width: 15px;
    height: 15px;
    margin-top: 1px;
    accent-color: #5b462a;
  }

  .dnd-newsletter-signup__actions {
    display: flex;
    align-items: center;
  }

  .dnd-newsletter-signup__actions .dnd-btn {
    width: auto;
    min-width: 138px;
    min-height: 42px;
    padding: 0 18px;
    border-radius: 999px;
    justify-content: center;
    font-size: 0.88rem;
  }

  .dnd-newsletter-signup__loading {
    display: none;
    width: auto;
    min-width: 138px;
    min-height: 42px;
    border: 0;
    border-radius: 999px;
    background: rgba(39, 32, 24, 0.1);
    color: rgba(39, 32, 24, 0.62);
    font: inherit;
  }

  .dnd-newsletter-signup__success {
    justify-content: flex-start;
  }

  @media (max-width: 900px) {
    .dnd-newsletter-signup {
      padding: 14px 0 22px;
    }

    .dnd-newsletter-signup__shell {
      padding: 18px 16px;
      border-radius: 16px;
    }

    .dnd-newsletter-signup__copy,
    .dnd-newsletter-signup__success {
      display: grid;
      justify-content: stretch;
      gap: 14px;
    }

    .dnd-newsletter-signup__form {
      width: 100%;
    }

    .dnd-newsletter-signup__form-row {
      display: grid;
      grid-template-columns: 1fr;
    }

    .dnd-newsletter-signup__field,
    .dnd-newsletter-signup__field input {
      width: 100%;
      min-width: 0;
    }

    .dnd-newsletter-signup__actions .dnd-btn,
    .dnd-newsletter-signup__loading {
      width: 100%;
      min-width: 0;
    }
  }

  @media (max-width: 640px) {
    .dnd-newsletter-signup {
      padding: 12px 0 18px;
    }

    .dnd-newsletter-signup h2 {
      font-size: clamp(1rem, 5vw, 1.15rem);
      line-height: 1.08;
    }

    .dnd-newsletter-signup__lead,
    .dnd-newsletter-signup__success p {
      font-size: 0.78rem;
      line-height: 1.34;
    }
  }
</style>
