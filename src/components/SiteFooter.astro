---
import { accommodations } from "../data/accommodations";

// [CHANGE 2026-05-20 00:00] English footer copy and safe English links added while preserving the Hungarian footer layout.
// [CHANGE 2026-08-11 19:00] Footer logo reduced and integrated into the dark brand block.
// [CHANGE 2026-08-11 19:30] Footer warmed with a subtle dandelion petal light treatment.
// [CHANGE 2026-08-11 20:48] Footer newsletter changed to email-first modal signup with Brevo POST handoff.
interface Props {
  lang?: "hu" | "en" | "de" | "sk" | "pl" | "cs";
  bookingHrefOverride?: string;
}

const { lang = "hu", bookingHrefOverride } = Astro.props as Props;
const isEnglishFooter = lang === "en";
const isGermanFooter = lang === "de";
const isCzechFooter = lang === "cs";
const isSlovakFooter = lang === "sk";
const brevoFormUrl =
  "https://d45cbe7c.sibforms.com/v2/serve/MUIFACKpKPrp0nwNuackNku75WN9hdMxMVAUPvYNULPddmR-aQ4pe1wxLyqg6u4d21Jo-blvV37HVRZKbvxuYftYLIkN6S7ajl9qCjOx18tAN6JKJwNJ_IFZ8T734t6kjJYrFHDFd5IInkVVxIBf41DiExIF0RJi2TBTLktGdUqc9bo-P7h-0a1ue324AI-1mna3XHb3kWqPfmpycQ==";
const footerNewsletterCopy = {
  hu: {
    title: "Iratkozzon fel a Dandelion hírlevelére",
    text: "Inspirációk, különleges ajánlatok és balatoni élmények közvetlenül e-mailben.",
    cta: "Feliratkozom a hírlevélre"
  },
  en: {
    title: "Subscribe to the Dandelion newsletter",
    text: "Inspiration, special offers and Balaton experiences delivered directly to your inbox.",
    cta: "Subscribe to the newsletter"
  },
  de: {
    title: "Für den Dandelion-Newsletter anmelden",
    text: "Inspirationen, besondere Angebote und Erlebnisse am Balaton direkt per E-Mail.",
    cta: "Zum Newsletter anmelden"
  },
  cs: {
    title: "Přihlaste se k odběru newsletteru Dandelion",
    text: "Inspirace, speciální nabídky a zážitky u Balatonu přímo e-mailem.",
    cta: "Přihlásit se k newsletteru"
  },
  sk: {
    title: "Prihláste sa na odber newslettera Dandelion",
    text: "Inšpirácie, špeciálne ponuky a zážitky pri Balatone priamo e-mailom.",
    cta: "Prihlásiť sa na newsletter"
  },
  pl: {
    title: "Zapisz się do newslettera Dandelion",
    text: "Inspiracje, wyjątkowe oferty i atrakcje nad Balatonem bezpośrednio na e-mail.",
    cta: "Zapisz się do newslettera"
  }
} as const;
const newsletterCopy = footerNewsletterCopy[lang] ?? footerNewsletterCopy.hu;
const newsletterFormCopy = {
  hu: {
    emailLabel: "E-mail-cím",
    emailPlaceholder: "E-mail-cím",
    openLabel: "Feliratkozás folytatása",
    dialogLead: "Ellenőrizze az e-mail-címét, majd erősítse meg a feliratkozást.",
    consent: "Hozzájárulok, hogy a Dandelion Vendégházak hírlevelet és marketingkommunikációt küldjön a megadott e-mail-címre.",
    submit: "Feliratkozom",
    close: "Bezárás",
    response: "A feliratkozás visszaigazolása"
  },
  en: {
    emailLabel: "Email address",
    emailPlaceholder: "Email address",
    openLabel: "Continue signup",
    dialogLead: "Check your email address, then confirm your newsletter signup.",
    consent: "I agree that Dandelion Guesthouses may send newsletters and marketing communications to the email address provided.",
    submit: "Subscribe",
    close: "Close",
    response: "Signup confirmation"
  },
  de: {
    emailLabel: "E-Mail-Adresse",
    emailPlaceholder: "E-Mail-Adresse",
    openLabel: "Anmeldung fortsetzen",
    dialogLead: "Prüfen Sie Ihre E-Mail-Adresse und bestätigen Sie anschließend die Anmeldung.",
    consent: "Ich stimme zu, dass Dandelion Gästehäuser Newsletter und Marketingmitteilungen an die angegebene E-Mail-Adresse sendet.",
    submit: "Anmelden",
    close: "Schließen",
    response: "Anmeldebestätigung"
  },
  cs: {
    emailLabel: "E-mailová adresa",
    emailPlaceholder: "E-mailová adresa",
    openLabel: "Pokračovat v přihlášení",
    dialogLead: "Zkontrolujte svou e-mailovou adresu a potvrďte přihlášení k newsletteru.",
    consent: "Souhlasím se zasíláním newsletteru a marketingové komunikace Dandelion Guesthouses na uvedenou e-mailovou adresu.",
    submit: "Přihlásit se",
    close: "Zavřít",
    response: "Potvrzení přihlášení"
  },
  sk: {
    emailLabel: "E-mailová adresa",
    emailPlaceholder: "E-mailová adresa",
    openLabel: "Pokračovať v prihlásení",
    dialogLead: "Skontrolujte svoju e-mailovú adresu a potvrďte prihlásenie na odber newslettera.",
    consent: "Súhlasím so zasielaním newslettera a marketingovej komunikácie Dandelion Guesthouses na uvedenú e-mailovú adresu.",
    submit: "Prihlásiť sa",
    close: "Zavrieť",
    response: "Potvrdenie prihlásenia"
  },
  pl: {
    emailLabel: "Adres e-mail",
    emailPlaceholder: "Adres e-mail",
    openLabel: "Kontynuuj zapis",
    dialogLead: "Sprawdź swój adres e-mail i potwierdź zapis do newslettera.",
    consent: "Wyrażam zgodę na otrzymywanie newslettera i komunikacji marketingowej Dandelion Guesthouses na podany adres e-mail.",
    submit: "Zapisz się",
    close: "Zamknij",
    response: "Potwierdzenie zapisu"
  }
} as const;
const newsletterUiCopy = newsletterFormCopy[lang] ?? newsletterFormCopy.hu;
const newsletterSuccessCopy = {
  hu: "Köszönjük! A feliratkozási kérését elküldtük. Kérjük, ellenőrizze az e-mail-fiókját a további teendőkért.",
  en: "Thank you! Your signup request has been sent. Please check your inbox for the next steps.",
  de: "Vielen Dank! Ihre Anmeldung wurde gesendet. Bitte prüfen Sie Ihren Posteingang für die nächsten Schritte.",
  cs: "Děkujeme! Vaše žádost o přihlášení byla odeslána. Další kroky najdete ve své e-mailové schránce.",
  sk: "Ďakujeme! Vaša žiadosť o prihlásenie bola odoslaná. Ďalšie kroky nájdete vo svojej e-mailovej schránke.",
  pl: "Dziękujemy! Zgłoszenie zostało wysłane. Sprawdź swoją skrzynkę e-mail, aby poznać dalsze kroki."
} as const;
const newsletterSuccess = newsletterSuccessCopy[lang] ?? newsletterSuccessCopy.hu;

const homeHref = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const footerLogoSrc = `${homeHref}images/LOGO/dandelion-logo-footer.svg`;

const resolveLocalHref = (href: string) => {
  if (href.startsWith("http")) {
    return href;
  }

  return href.startsWith("/") ? `${homeHref}${href.replace(/^\/+/, "")}` : `${homeHref}${href}`;
};

const buildStayHref = (slug: string, url: string) =>
  slug === "dandelion-d2"
    ? `${homeHref}dandelion-d2/`
    : slug === "fugehaz"
      ? `${homeHref}fuge/`
      : slug === "royal-homes"
        ? `${homeHref}royal/`
        : resolveLocalHref(url);

const footerStayLabelOverrides: Record<string, string> = {
  fugehaz: "Dandelion Fügeház",
  "vintage-vendeghaz": "Dandelion Vintage"
};

const footerStayBySlug = new Map(
  accommodations.map((stay) => [
    stay.slug,
    {
      label: footerStayLabelOverrides[stay.slug] ?? stay.name,
      href: buildStayHref(stay.slug, stay.url)
    }
  ])
);

const footerStayColumns = [
  ["dandelion-d1", "dandelion-d2", "fugehaz", "szololiget-vendeghaz"],
  ["zsalya-vendeghaz", "vintage-vendeghaz", "royal-homes", "szepvolgyi-vendeghaz", "dandelion-koveskal"]
].map((column) =>
  column
    .map((slug) => footerStayBySlug.get(slug))
    .filter((stay): stay is { label: string; href: string } => Boolean(stay))
);

const englishStayHrefOverrides: Record<string, string> = {
  "dandelion-d1": `${homeHref}en/dandelion-d1/`,
  "dandelion-d2": `${homeHref}en/dandelion-d2/`,
  fugehaz: `${homeHref}en/dandelion-fugehaz/`,
  "szololiget-vendeghaz": `${homeHref}en/szololiget/`,
  "zsalya-vendeghaz": `${homeHref}en/dandelion-zsalya/`,
  "vintage-vendeghaz": `${homeHref}en/dandelion-vintage/`,
  "royal-homes": `${homeHref}en/royal/`,
  "szepvolgyi-vendeghaz": `${homeHref}en/szepvolgyi/`,
  "dandelion-koveskal": `${homeHref}en/dandelion-koveskal/`
};

const englishStayLabelOverrides: Record<string, string> = {
  fugehaz: "Dandelion Fügeház",
  "zsalya-vendeghaz": "Dandelion Zsálya",
  "szololiget-vendeghaz": "Dandelion Szőlőliget",
  "szepvolgyi-vendeghaz": "Dandelion Szépvölgyi",
  "vintage-vendeghaz": "Dandelion Vintage"
};

const englishFooterStayBySlug = new Map(
  accommodations.map((stay) => [
    stay.slug,
    {
      label: englishStayLabelOverrides[stay.slug] ?? footerStayLabelOverrides[stay.slug] ?? stay.name,
      href: englishStayHrefOverrides[stay.slug] ?? buildStayHref(stay.slug, stay.url)
    }
  ])
);

const englishFooterStayColumns = [
  ["dandelion-d1", "dandelion-d2", "fugehaz", "szololiget-vendeghaz"],
  ["zsalya-vendeghaz", "vintage-vendeghaz", "royal-homes", "szepvolgyi-vendeghaz", "dandelion-koveskal"]
].map((column) =>
  column
    .map((slug) => englishFooterStayBySlug.get(slug))
    .filter((stay): stay is { label: string; href: string } => Boolean(stay))
);

const germanStayLabelOverrides: Record<string, string> = {
  fugehaz: "Dandelion Fügeház",
  "zsalya-vendeghaz": "Dandelion Zsálya",
  "szololiget-vendeghaz": "Dandelion Szőlőliget",
  "szepvolgyi-vendeghaz": "Dandelion Szépvölgyi",
  "vintage-vendeghaz": "Dandelion Vintage"
};

const germanStayHrefOverrides: Record<string, string> = {
  "dandelion-d1": `${homeHref}de/dandelion-d1/`,
  "dandelion-d2": `${homeHref}de/dandelion-d2/`,
  fugehaz: `${homeHref}de/dandelion-fugehaz/`,
  "zsalya-vendeghaz": `${homeHref}de/dandelion-zsalya/`,
  "szololiget-vendeghaz": `${homeHref}de/szololiget/`,
  "szepvolgyi-vendeghaz": `${homeHref}de/szepvolgyi/`,
  "royal-homes": `${homeHref}de/royal/`,
  "vintage-vendeghaz": `${homeHref}de/dandelion-vintage/`,
  "dandelion-koveskal": `${homeHref}de/dandelion-koveskal/`
};

const germanFooterStayBySlug = new Map(
  accommodations.map((stay) => [
    stay.slug,
    {
      label: germanStayLabelOverrides[stay.slug] ?? footerStayLabelOverrides[stay.slug] ?? stay.name,
      href: germanStayHrefOverrides[stay.slug] ?? `${homeHref}de/unterkuenfte/#${stay.slug}`
    }
  ])
);

const germanFooterStayColumns = [
  ["dandelion-d1", "dandelion-d2", "fugehaz", "szololiget-vendeghaz"],
  ["zsalya-vendeghaz", "vintage-vendeghaz", "royal-homes", "szepvolgyi-vendeghaz", "dandelion-koveskal"]
].map((column) =>
  column
    .map((slug) => germanFooterStayBySlug.get(slug))
    .filter((stay): stay is { label: string; href: string } => Boolean(stay))
);

const czechStayLabelOverrides: Record<string, string> = {
  fugehaz: "Dandelion Fügeház",
  "zsalya-vendeghaz": "Dandelion Zsálya",
  "szololiget-vendeghaz": "Dandelion Szőlőliget",
  "szepvolgyi-vendeghaz": "Dandelion Szépvölgyi",
  "vintage-vendeghaz": "Dandelion Vintage"
};

const czechStayHrefOverrides: Record<string, string> = {
  "dandelion-d1": `${homeHref}cs/dandelion-d1/`,
  "dandelion-d2": `${homeHref}cs/dandelion-d2/`,
  fugehaz: `${homeHref}cs/dandelion-fugehaz/`,
  "zsalya-vendeghaz": `${homeHref}cs/dandelion-zsalya/`,
  "szololiget-vendeghaz": `${homeHref}cs/szololiget/`,
  "szepvolgyi-vendeghaz": `${homeHref}cs/szepvolgyi/`,
  "royal-homes": `${homeHref}cs/royal/`,
  "vintage-vendeghaz": `${homeHref}cs/dandelion-vintage/`,
  "dandelion-koveskal": `${homeHref}cs/dandelion-koveskal/`
};

const czechFooterStayBySlug = new Map(
  accommodations.map((stay) => [
    stay.slug,
    {
      label: czechStayLabelOverrides[stay.slug] ?? footerStayLabelOverrides[stay.slug] ?? stay.name,
      href: czechStayHrefOverrides[stay.slug] ?? `${homeHref}cs/ubytovani/#${stay.slug}`
    }
  ])
);

const czechFooterStayColumns = [
  ["dandelion-d1", "dandelion-d2", "fugehaz", "szololiget-vendeghaz"],
  ["zsalya-vendeghaz", "vintage-vendeghaz", "royal-homes", "szepvolgyi-vendeghaz", "dandelion-koveskal"]
].map((column) =>
  column
    .map((slug) => czechFooterStayBySlug.get(slug))
    .filter((stay): stay is { label: string; href: string } => Boolean(stay))
);

const slovakStayLabelOverrides: Record<string, string> = {
  fugehaz: "Dandelion Fügeház",
  "zsalya-vendeghaz": "Dandelion Zsálya",
  "szololiget-vendeghaz": "Dandelion Szőlőliget",
  "szepvolgyi-vendeghaz": "Dandelion Szépvölgyi",
  "vintage-vendeghaz": "Dandelion Vintage"
};

const slovakStayHrefOverrides: Record<string, string> = {
  "dandelion-d1": `${homeHref}sk/dandelion-d1/`,
  "dandelion-d2": `${homeHref}sk/dandelion-d2/`,
  fugehaz: `${homeHref}sk/dandelion-fugehaz/`,
  "zsalya-vendeghaz": `${homeHref}sk/dandelion-zsalya/`,
  "szololiget-vendeghaz": `${homeHref}sk/szololiget/`,
  "szepvolgyi-vendeghaz": `${homeHref}sk/szepvolgyi/`,
  "royal-homes": `${homeHref}sk/royal/`,
  "vintage-vendeghaz": `${homeHref}sk/dandelion-vintage/`,
  "dandelion-koveskal": `${homeHref}sk/dandelion-koveskal/`
};

const slovakFooterStayBySlug = new Map(
  accommodations.map((stay) => [
    stay.slug,
    {
      label: slovakStayLabelOverrides[stay.slug] ?? footerStayLabelOverrides[stay.slug] ?? stay.name,
      href: slovakStayHrefOverrides[stay.slug] ?? `${homeHref}sk/ubytovanie/#${stay.slug}`
    }
  ])
);

const slovakFooterStayColumns = [
  ["dandelion-d1", "dandelion-d2", "fugehaz", "szololiget-vendeghaz"],
  ["zsalya-vendeghaz", "vintage-vendeghaz", "royal-homes", "szepvolgyi-vendeghaz", "dandelion-koveskal"]
].map((column) =>
  column
    .map((slug) => slovakFooterStayBySlug.get(slug))
    .filter((stay): stay is { label: string; href: string } => Boolean(stay))
);

const bookingLanguage = isEnglishFooter || isGermanFooter || isCzechFooter || isSlovakFooter ? "En" : "Hu";
const defaultBookingHref = `https://ibe.sabeeapp.com/v3/p/Dandelion-Vendégházak?p=3970b30e1042d58f&lang=${bookingLanguage}`;
const bookingHref = bookingHrefOverride ?? defaultBookingHref;
const resolvedBookingHref = bookingHref;
const englishFooterNav = [
  { label: "Home", href: `${homeHref}en/` },
  { label: "Accommodations", href: `${homeHref}en/szallasok/` },
  { label: "Experiences", href: `${homeHref}en/experiences/` },
  { label: "Contact", href: `${homeHref}en/contact/` }
];
const englishLanguageLinks = [
  { label: "HU", href: homeHref },
  { label: "EN", href: `${homeHref}en/` },
  { label: "DE", href: `${homeHref}de/` },
  { label: "CZ", href: `${homeHref}cs/` },
  { label: "SK", href: `${homeHref}sk/` }
];
const englishLegalLinks = [
  { label: "Terms and Conditions", href: `${homeHref}en/terms-and-conditions/` },
  { label: "Privacy Policy", href: `${homeHref}en/privacy-policy/` },
  { label: "Imprint", href: `${homeHref}en/imprint/` }
];
const germanFooterNav = [
  { label: "Startseite", href: `${homeHref}de/` },
  { label: "Unterkünfte", href: `${homeHref}de/unterkuenfte/` },
  { label: "Erlebnisse", href: `${homeHref}de/erlebnisse/` },
  { label: "Kontakt", href: `${homeHref}de/kontakt/` }
];
const germanLanguageLinks = [
  { label: "HU", href: homeHref },
  { label: "EN", href: `${homeHref}en/` },
  { label: "DE", href: `${homeHref}de/` },
  { label: "CZ", href: `${homeHref}cs/` },
  { label: "SK", href: `${homeHref}sk/` }
];
const germanLegalLinks = [
  { label: "AGB", href: `${homeHref}de/agb/` },
  { label: "Datenschutz", href: `${homeHref}de/datenschutz/` },
  { label: "Impressum", href: `${homeHref}de/impressum/` }
];
const czechFooterNav = [
  { label: "Úvod", href: `${homeHref}cs/` },
  { label: "Ubytování", href: `${homeHref}cs/ubytovani/` },
  { label: "Zážitky", href: `${homeHref}cs/zazitky/` },
  { label: "Kontakt", href: `${homeHref}cs/kontakt/` }
];
const czechLanguageLinks = [
  { label: "HU", href: homeHref },
  { label: "EN", href: `${homeHref}en/` },
  { label: "DE", href: `${homeHref}de/` },
  { label: "CZ", href: `${homeHref}cs/` },
  { label: "SK", href: `${homeHref}sk/` }
];
const czechLegalLinks = [
  { label: "Obchodní podmínky", href: `${homeHref}cs/obchodni-podminky/` },
  { label: "Ochrana osobních údajů", href: `${homeHref}cs/ochrana-osobnich-udaju/` },
  { label: "Tiráž", href: `${homeHref}cs/tiraz/` }
];
const slovakFooterNav = [
  { label: "Úvod", href: `${homeHref}sk/` },
  { label: "Ubytovanie", href: `${homeHref}sk/ubytovanie/` },
  { label: "Zážitky", href: `${homeHref}sk/zazitky/` },
  { label: "Kontakt", href: `${homeHref}sk/kontakt/` }
];
const slovakLanguageLinks = [
  { label: "HU", href: homeHref },
  { label: "EN", href: `${homeHref}en/` },
  { label: "DE", href: `${homeHref}de/` },
  { label: "CZ", href: `${homeHref}cs/` },
  { label: "SK", href: `${homeHref}sk/` }
];
const slovakLegalLinks = [
  { label: "Obchodné podmienky", href: `${homeHref}sk/obchodne-podmienky/` },
  { label: "Ochrana osobných údajov", href: `${homeHref}sk/ochrana-osobnych-udajov/` },
  { label: "Tiráž", href: `${homeHref}sk/tiraz/` }
];
---

{isSlovakFooter && (
  <footer class="dnd-site-footer" aria-labelledby="site-footer-title">
    <div class="dnd-container dnd-site-footer__newsletter" aria-labelledby="footer-newsletter-title">
      <div>
        <h2 id="footer-newsletter-title">{newsletterCopy.title}</h2>
        <p>{newsletterCopy.text}</p>
      </div>
    <form class="dnd-site-footer__newsletter-form" data-newsletter-trigger>
      <label class="dnd-sr-only" for={`footer-newsletter-email-${lang}`}>{newsletterUiCopy.emailLabel}</label>
      <input id={`footer-newsletter-email-${lang}`} name="email" type="email" placeholder={newsletterUiCopy.emailPlaceholder} autocomplete="email" required />
      <button type="submit" aria-label={newsletterUiCopy.openLabel}>
        <span aria-hidden="true">→</span>
      </button>
    </form>
    </div>

    <div class="dnd-container dnd-site-footer__inner">
      <div class="dnd-site-footer__brand">
        <a href={`${homeHref}sk/`} class="dnd-site-footer__brand-link" aria-label="Dandelion ubytovanie úvod">
          <img src={footerLogoSrc} alt="Dandelion ubytovanie" width="220" height="134" />
        </a>
        <p id="site-footer-title">Pokojné penzióny a apartmány pri Balatone a v Balatonskej vrchovine.</p>
      </div>

      <nav class="dnd-site-footer__group dnd-site-footer__group--stays" aria-label="Ubytovanie">
        <h2>Ubytovanie</h2>
        <div class="dnd-site-footer__stay-columns">
          {slovakFooterStayColumns.map((column) => (
            <ul>
              {column.map((stay) => (
                <li><a href={stay.href}>{stay.label}</a></li>
              ))}
            </ul>
          ))}
        </div>
      </nav>

      <nav class="dnd-site-footer__group" aria-label="Užitočné odkazy">
        <h2>Užitočné odkazy</h2>
        <ul>
          {slovakFooterNav.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
          <li><a href={resolvedBookingHref}>Overiť dostupnosť</a></li>
          <li><a href={resolvedBookingHref}>Rezervácia</a></li>
        </ul>
      </nav>

      <nav class="dnd-site-footer__group" aria-label="Jazyky">
        <h2>Jazyky</h2>
        <ul>
          {slovakLanguageLinks.map((item) => (
            <li class="dnd-site-footer__language-link"><a href={item.href}>{item.label}</a></li>
          ))}
        </ul>
      </nav>

      <nav class="dnd-site-footer__group" aria-label="Právne informácie">
        <h2>Právne</h2>
        <ul>
          {slovakLegalLinks.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
        </ul>
      </nav>
    </div>

    <div class="dnd-container dnd-site-footer__bottom">
      <span>© Dandelion ubytovanie</span>
    </div>
  </footer>
)}

{isCzechFooter && (
  <footer class="dnd-site-footer" aria-labelledby="site-footer-title">
    <div class="dnd-container dnd-site-footer__newsletter" aria-labelledby="footer-newsletter-title">
      <div>
        <h2 id="footer-newsletter-title">{newsletterCopy.title}</h2>
        <p>{newsletterCopy.text}</p>
      </div>
    <form class="dnd-site-footer__newsletter-form" data-newsletter-trigger>
      <label class="dnd-sr-only" for={`footer-newsletter-email-${lang}`}>{newsletterUiCopy.emailLabel}</label>
      <input id={`footer-newsletter-email-${lang}`} name="email" type="email" placeholder={newsletterUiCopy.emailPlaceholder} autocomplete="email" required />
      <button type="submit" aria-label={newsletterUiCopy.openLabel}>
        <span aria-hidden="true">→</span>
      </button>
    </form>
    </div>

    <div class="dnd-container dnd-site-footer__inner">
      <div class="dnd-site-footer__brand">
        <a href={`${homeHref}cs/`} class="dnd-site-footer__brand-link" aria-label="Dandelion ubytování úvod">
          <img src={footerLogoSrc} alt="Dandelion ubytování" width="220" height="134" />
        </a>
        <p id="site-footer-title">Klidné penziony a apartmány u Balatonu a v Balatonské vrchovině.</p>
      </div>

      <nav class="dnd-site-footer__group dnd-site-footer__group--stays" aria-label="Ubytování">
        <h2>Ubytování</h2>
        <div class="dnd-site-footer__stay-columns">
          {czechFooterStayColumns.map((column) => (
            <ul>
              {column.map((stay) => (
                <li><a href={stay.href}>{stay.label}</a></li>
              ))}
            </ul>
          ))}
        </div>
      </nav>

      <nav class="dnd-site-footer__group" aria-label="Užitečné odkazy">
        <h2>Užitečné odkazy</h2>
        <ul>
          {czechFooterNav.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
          <li><a href={resolvedBookingHref}>Ověřit dostupnost</a></li>
          <li><a href={resolvedBookingHref}>Rezervace</a></li>
        </ul>
      </nav>

      <nav class="dnd-site-footer__group" aria-label="Jazyky">
        <h2>Jazyky</h2>
        <ul>
          {czechLanguageLinks.map((item) => (
            <li class="dnd-site-footer__language-link"><a href={item.href}>{item.label}</a></li>
          ))}
        </ul>
      </nav>

      <nav class="dnd-site-footer__group" aria-label="Právní informace">
        <h2>Právní</h2>
        <ul>
          {czechLegalLinks.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
        </ul>
      </nav>
    </div>

    <div class="dnd-container dnd-site-footer__bottom">
      <span>© Dandelion ubytování</span>
    </div>
  </footer>
)}

{isGermanFooter && (
  <footer class="dnd-site-footer" aria-labelledby="site-footer-title">
    <div class="dnd-container dnd-site-footer__newsletter" aria-labelledby="footer-newsletter-title">
      <div>
        <h2 id="footer-newsletter-title">{newsletterCopy.title}</h2>
        <p>{newsletterCopy.text}</p>
      </div>
    <form class="dnd-site-footer__newsletter-form" data-newsletter-trigger>
      <label class="dnd-sr-only" for={`footer-newsletter-email-${lang}`}>{newsletterUiCopy.emailLabel}</label>
      <input id={`footer-newsletter-email-${lang}`} name="email" type="email" placeholder={newsletterUiCopy.emailPlaceholder} autocomplete="email" required />
      <button type="submit" aria-label={newsletterUiCopy.openLabel}>
        <span aria-hidden="true">→</span>
      </button>
    </form>
    </div>

    <div class="dnd-container dnd-site-footer__inner">
      <div class="dnd-site-footer__brand">
        <a href={`${homeHref}de/`} class="dnd-site-footer__brand-link" aria-label="Dandelion Gästehäuser Startseite">
          <img src={footerLogoSrc} alt="Dandelion Gästehäuser" width="220" height="134" />
        </a>
        <p id="site-footer-title">Ruhige Gästehäuser und Apartments im Balaton-Oberland.</p>
      </div>

      <nav class="dnd-site-footer__group dnd-site-footer__group--stays" aria-label="Unterkünfte">
        <h2>Unterkünfte</h2>
        <div class="dnd-site-footer__stay-columns">
          {germanFooterStayColumns.map((column) => (
            <ul>
              {column.map((stay) => (
                <li><a href={stay.href}>{stay.label}</a></li>
              ))}
            </ul>
          ))}
        </div>
      </nav>

      <nav class="dnd-site-footer__group" aria-label="Buchung">
        <h2>Nützliche Links</h2>
        <ul>
          {germanFooterNav.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
          <li><a href={resolvedBookingHref}>Verfügbarkeit prüfen</a></li>
          <li><a href={resolvedBookingHref}>Jetzt buchen</a></li>
        </ul>
      </nav>

      <nav class="dnd-site-footer__group" aria-label="Sprachen">
        <h2>Sprachen</h2>
        <ul>
          {germanLanguageLinks.map((item) => (
            <li class="dnd-site-footer__language-link"><a href={item.href}>{item.label}</a></li>
          ))}
        </ul>
      </nav>

      <nav class="dnd-site-footer__group" aria-label="Rechtliches">
        <h2>Rechtliches</h2>
        <ul>
          {germanLegalLinks.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
        </ul>
      </nav>
    </div>

    <div class="dnd-container dnd-site-footer__bottom">
      <span>© Dandelion Gästehäuser</span>
    </div>
  </footer>
)}

{!isGermanFooter && !isCzechFooter && !isSlovakFooter && (
<footer class="dnd-site-footer" aria-labelledby="site-footer-title">
  <div class="dnd-container dnd-site-footer__newsletter" aria-labelledby="footer-newsletter-title">
    <div>
      <h2 id="footer-newsletter-title">{newsletterCopy.title}</h2>
      <p>{newsletterCopy.text}</p>
    </div>
    <form class="dnd-site-footer__newsletter-form" data-newsletter-trigger>
      <label class="dnd-sr-only" for={`footer-newsletter-email-${lang}`}>{newsletterUiCopy.emailLabel}</label>
      <input id={`footer-newsletter-email-${lang}`} name="email" type="email" placeholder={newsletterUiCopy.emailPlaceholder} autocomplete="email" required />
      <button type="submit" aria-label={newsletterUiCopy.openLabel}>
        <span aria-hidden="true">→</span>
      </button>
    </form>
  </div>

  <div class="dnd-container dnd-site-footer__inner">
    <div class="dnd-site-footer__brand">
      <a href={isEnglishFooter ? `${homeHref}en/` : homeHref} class="dnd-site-footer__brand-link" aria-label={isEnglishFooter ? "Dandelion Guesthouses home" : "Dandelion Vendégházak főoldal"}>
        <img src={footerLogoSrc} alt={isEnglishFooter ? "Dandelion Guesthouses" : "Dandelion Vendégházak"} width="220" height="134" />
      </a>
      <p id="site-footer-title">
        {isEnglishFooter
          ? "Peaceful countryside guesthouses and apartments in the Balaton Uplands."
          : "Természetközeli vendégházak a Balaton-felvidéken, családoknak, pároknak és nyugodt elvonuláshoz."}
      </p>
    </div>

    {isEnglishFooter ? (
      <nav class="dnd-site-footer__group dnd-site-footer__group--stays" aria-label="Accommodations">
        <h2>Accommodations</h2>
        <div class="dnd-site-footer__stay-columns">
          {englishFooterStayColumns.map((column) => (
            <ul>
              {column.map((stay) => (
                <li><a href={stay.href}>{stay.label}</a></li>
              ))}
            </ul>
          ))}
        </div>
      </nav>
    ) : (
      <nav class="dnd-site-footer__group dnd-site-footer__group--stays" aria-label="Szállások">
        <h2>Szállások</h2>
        <div class="dnd-site-footer__stay-columns">
          {footerStayColumns.map((column) => (
            <ul>
              {column.map((stay) => (
                <li><a href={stay.href}>{stay.label}</a></li>
              ))}
            </ul>
          ))}
        </div>
      </nav>
    )}

    {isEnglishFooter ? (
      <nav class="dnd-site-footer__group" aria-label="Booking">
        <h2>Useful links</h2>
        <ul>
          {englishFooterNav.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
          <li><a href={bookingHref}>Check availability</a></li>
          <li><a href={bookingHref}>Book now</a></li>
        </ul>
      </nav>
    ) : (
      <nav class="dnd-site-footer__group" aria-label="Hasznos linkek">
        <h2>Hasznos linkek</h2>
        <ul>
          <li><a href={resolveLocalHref("/")}>Főoldal</a></li>
          <li><a href={resolveLocalHref("/szallasok/")}>Szállások</a></li>
          <li><a href={resolveLocalHref("/#elmenyek")}>Élmények</a></li>
          <li><a href={resolveLocalHref("/kapcsolat/")}>Kapcsolat</a></li>
          <li><a href={bookingHref}>Foglalás</a></li>
        </ul>
      </nav>
    )}

    {isEnglishFooter ? (
      <nav class="dnd-site-footer__group" aria-label="Languages">
        <h2>Legal</h2>
        <ul>
          {englishLegalLinks.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
          {englishLanguageLinks.map((item) => (
            <li class="dnd-site-footer__language-link"><a href={item.href}>{item.label}</a></li>
          ))}
        </ul>
      </nav>
    ) : (
      <nav class="dnd-site-footer__group" aria-label="Jogi információk">
        <h2>Jogi</h2>
        <ul>
          <li><a href={resolveLocalHref("/aszf/")}>ÁSZF</a></li>
          <li><a href={resolveLocalHref("/adatkezelesi-tajekoztato/")}>Adatkezelési tájékoztató</a></li>
          <li><a href={resolveLocalHref("/impresszum/")}>Impresszum</a></li>
        </ul>
      </nav>
    )}
  </div>

  <div class="dnd-container dnd-site-footer__bottom">
    <span>© {isEnglishFooter ? "Dandelion Guesthouses" : "Dandelion Vendégházak"}</span>
  </div>
</footer>
)}

<dialog class="dnd-newsletter-dialog" data-newsletter-dialog aria-labelledby={`newsletter-dialog-title-${lang}`}>
  <div class="dnd-newsletter-dialog__panel">
    <button class="dnd-newsletter-dialog__close" type="button" data-newsletter-close aria-label={newsletterUiCopy.close}>×</button>
    <span class="dnd-newsletter-signup__kicker">{newsletterCopy.title}</span>
    <h2 id={`newsletter-dialog-title-${lang}`}>{newsletterCopy.title}</h2>
    <p class="dnd-newsletter-dialog__lead">{newsletterUiCopy.dialogLead}</p>

    <form
      class="dnd-newsletter-dialog__form"
      action={brevoFormUrl}
      method="POST"
      target="dnd-newsletter-response"
      data-newsletter-submit
    >
      <label for={`newsletter-dialog-email-${lang}`}>{newsletterUiCopy.emailLabel}</label>
      <input id={`newsletter-dialog-email-${lang}`} name="EMAIL" type="email" autocomplete="email" required data-newsletter-email />
      <label class="dnd-newsletter-dialog__consent">
        <input type="checkbox" name="OPT_IN" value="1" required />
        <span>{newsletterUiCopy.consent}</span>
      </label>
      <input type="hidden" name="email_address_check" value="" />
      <input type="hidden" name="locale" value="en" />
      <button type="submit">{newsletterUiCopy.submit}</button>
    </form>

    <p class="dnd-newsletter-dialog__success" data-newsletter-success role="status" tabindex="-1" hidden>{newsletterSuccess}</p>

    <iframe
      class="dnd-newsletter-dialog__response"
      name="dnd-newsletter-response"
      title={newsletterUiCopy.response}
      data-newsletter-response
      hidden
    ></iframe>
  </div>
</dialog>

<style>
  .dnd-site-footer {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    margin-top: 32px;
    padding: 56px 0 22px;
    background: linear-gradient(180deg, #2b2118 0%, #1e1711 100%);
    color: rgba(253, 251, 247, 0.88);
    border-top: 1px solid rgba(217, 158, 43, 0.14);
  }

  .dnd-site-footer::before {
    position: absolute;
    top: 54px;
    right: 5%;
    width: 330px;
    height: 270px;
    border-radius: 50%;
    background:
      radial-gradient(ellipse 6px 24px at 50% 8%, rgba(249, 203, 91, 0.32) 0 48%, transparent 58%),
      radial-gradient(ellipse 5px 21px at 68% 17%, rgba(249, 203, 91, 0.24) 0 48%, transparent 58%),
      radial-gradient(ellipse 5px 21px at 83% 38%, rgba(249, 203, 91, 0.2) 0 48%, transparent 58%),
      radial-gradient(ellipse 5px 20px at 78% 65%, rgba(249, 203, 91, 0.18) 0 48%, transparent 58%),
      radial-gradient(ellipse 5px 21px at 60% 83%, rgba(249, 203, 91, 0.16) 0 48%, transparent 58%),
      radial-gradient(ellipse 5px 20px at 35% 86%, rgba(249, 203, 91, 0.17) 0 48%, transparent 58%),
      radial-gradient(ellipse 5px 21px at 16% 66%, rgba(249, 203, 91, 0.18) 0 48%, transparent 58%),
      radial-gradient(ellipse 5px 21px at 10% 40%, rgba(249, 203, 91, 0.2) 0 48%, transparent 58%),
      radial-gradient(ellipse 5px 22px at 28% 16%, rgba(249, 203, 91, 0.24) 0 48%, transparent 58%),
      radial-gradient(circle at 50% 49%, rgba(255, 216, 101, 0.62) 0 4px, transparent 5px),
      radial-gradient(circle at 50% 49%, rgba(249, 203, 91, 0.12), transparent 42%);
    opacity: 0.72;
    pointer-events: none;
    transform: rotate(-12deg);
    z-index: 0;
  }

  .dnd-site-footer::after {
    position: absolute;
    top: 0;
    right: 0;
    left: 0;
    height: 250px;
    background: linear-gradient(180deg, rgba(249, 203, 91, 0.08), transparent 82%);
    content: "";
    pointer-events: none;
    z-index: 0;
  }

  .dnd-site-footer > * {
    position: relative;
    z-index: 1;
  }

  .dnd-site-footer__inner {
    display: grid;
    grid-template-columns: minmax(0, 1.35fr) repeat(3, minmax(0, 0.85fr));
    gap: clamp(28px, 4vw, 48px);
    align-items: start;
  }

  .dnd-site-footer__brand {
    position: relative;
    max-width: 360px;
    padding: 10px 18px 14px;
    border-left: 1px solid rgba(249, 203, 91, 0.24);
    border-radius: 0 16px 16px 0;
    background: linear-gradient(135deg, rgba(249, 203, 91, 0.08), rgba(249, 203, 91, 0));
  }

  .dnd-site-footer__brand-link {
    display: inline-flex;
    margin-bottom: 14px;
    width: max-content;
    max-width: 100%;
    padding: 0;
    border-radius: 0;
    background: transparent;
  }

  .dnd-site-footer__brand-link img {
    display: block;
    width: min(190px, 100%);
    height: auto;
    filter: brightness(1.28) contrast(1.04);
  }

  .dnd-site-footer__brand p {
    margin: 0;
    color: rgba(253, 251, 247, 0.72);
    font-size: var(--dnd-text-md);
    line-height: 1.7;
  }

  .dnd-site-footer__group h2 {
    margin: 0 0 14px;
    color: rgba(217, 158, 43, 0.96);
    font-size: var(--dnd-text-xs);
    font-weight: 700;
    letter-spacing: var(--dnd-tracking-wide);
    line-height: 1.4;
    text-transform: uppercase;
  }

  .dnd-site-footer__group ul {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .dnd-site-footer__stay-columns {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px 24px;
  }

  .dnd-site-footer__group li + li {
    margin-top: 10px;
  }

  .dnd-site-footer__group a {
    display: inline-flex;
    color: rgba(253, 251, 247, 0.82);
    font-size: var(--dnd-text-md);
    line-height: 1.45;
    transition: color 0.18s ease, transform 0.18s ease;
  }

  .dnd-site-footer__group a:hover,
  .dnd-site-footer__group a:focus-visible {
    color: #fdfbf7;
    transform: translateX(1px);
  }

  .dnd-site-footer__bottom {
    margin-top: 34px;
    padding-top: 16px;
    border-top: 1px solid rgba(253, 251, 247, 0.08);
    color: rgba(253, 251, 247, 0.58);
    font-size: var(--dnd-text-xs);
    line-height: 1.45;
  }

  .dnd-site-footer__newsletter {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    margin-bottom: 34px;
    padding: 24px clamp(20px, 4vw, 42px);
    border: 1px solid rgba(217, 158, 43, 0.5);
    border-radius: 8px;
    background: rgba(217, 158, 43, 0.1);
  }

  .dnd-site-footer__newsletter h2 {
    margin: 0 0 6px;
    color: rgba(253, 251, 247, 0.92);
    font-size: clamp(1.08rem, 1.8vw, 1.3rem);
    font-weight: 600;
    line-height: 1.3;
  }

  .dnd-site-footer__newsletter p {
    margin: 0;
    color: rgba(253, 251, 247, 0.64);
    font-size: var(--dnd-text-md);
    line-height: 1.55;
  }

  .dnd-site-footer__newsletter-form {
    display: inline-flex;
    align-items: stretch;
    min-height: 48px;
    justify-content: center;
    flex: 0 0 auto;
    width: min(100%, 360px);
  }

  .dnd-site-footer__newsletter-form input {
    min-width: 0;
    flex: 1 1 auto;
    padding: 0 14px;
    border: 1px solid rgba(217, 158, 43, 0.78);
    border-right: 0;
    border-radius: 5px;
    border-top-right-radius: 0;
    border-bottom-right-radius: 0;
    background: rgba(255, 252, 245, 0.98);
    color: #201913;
    font-size: var(--dnd-text-md);
    line-height: 1.2;
  }

  .dnd-site-footer__newsletter-form input::placeholder {
    color: rgba(32, 25, 19, 0.58);
  }

  .dnd-site-footer__newsletter-form input:focus-visible,
  .dnd-site-footer__newsletter-form button:focus-visible {
    outline: 2px solid #f1bd54;
    outline-offset: 2px;
  }

  .dnd-site-footer__newsletter-form button {
    display: inline-flex;
    width: 52px;
    align-items: center;
    justify-content: center;
    flex: 0 0 52px;
    border: 1px solid #d99e2b;
    border-radius: 5px;
    border-top-left-radius: 0;
    border-bottom-left-radius: 0;
    background: #d99e2b;
    color: #201913;
    cursor: pointer;
    font-size: 1.35rem;
    font-weight: 700;
    line-height: 1;
  }

  .dnd-site-footer__newsletter-form button:hover,
  .dnd-site-footer__newsletter-form button:focus-visible {
    background: #f1bd54;
    color: #201913;
  }

  .dnd-sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .dnd-newsletter-dialog {
    width: min(92vw, 540px);
    padding: 0;
    border: 0;
    border-radius: 18px;
    background: transparent;
    color: #272018;
  }

  .dnd-newsletter-dialog::backdrop {
    background: rgba(23, 17, 12, 0.72);
    backdrop-filter: blur(3px);
  }

  .dnd-newsletter-dialog__panel {
    position: relative;
    padding: 32px;
    border: 1px solid rgba(217, 158, 43, 0.42);
    border-radius: 18px;
    background: #fffdf8;
    box-shadow: 0 24px 70px rgba(16, 10, 5, 0.3);
  }

  .dnd-newsletter-dialog__close {
    position: absolute;
    top: 14px;
    right: 14px;
    width: 34px;
    height: 34px;
    border: 1px solid rgba(39, 32, 24, 0.16);
    border-radius: 50%;
    background: transparent;
    color: rgba(39, 32, 24, 0.72);
    cursor: pointer;
    font-size: 1.35rem;
    line-height: 1;
  }

  .dnd-newsletter-dialog h2 {
    margin: 8px 42px 8px 0;
    font-family: var(--dnd-font-display);
    font-size: clamp(1.45rem, 3vw, 2rem);
    font-weight: 400;
    line-height: 1.1;
  }

  .dnd-newsletter-dialog__lead {
    margin: 0 0 22px;
    color: rgba(39, 32, 24, 0.7);
    font-size: var(--dnd-text-md);
    line-height: 1.5;
  }

  .dnd-newsletter-dialog__success {
    margin: 22px 0 0;
    padding: 18px;
    border: 1px solid rgba(84, 123, 78, 0.28);
    border-radius: 8px;
    background: rgba(226, 241, 220, 0.72);
    color: #31532e;
    font-size: var(--dnd-text-md);
    line-height: 1.55;
  }

  .dnd-newsletter-dialog__form {
    display: grid;
    gap: 10px;
  }

  .dnd-newsletter-dialog__form > label:first-child {
    color: rgba(39, 32, 24, 0.8);
    font-size: var(--dnd-text-sm);
    font-weight: 600;
  }

  .dnd-newsletter-dialog__form input[type="email"] {
    width: 100%;
    min-height: 48px;
    padding: 0 14px;
    border: 1px solid rgba(91, 70, 42, 0.28);
    border-radius: 6px;
    background: #fff;
    color: #272018;
    font-size: var(--dnd-text-md);
  }

  .dnd-newsletter-dialog__consent {
    display: flex;
    align-items: flex-start;
    gap: 9px;
    color: rgba(39, 32, 24, 0.7);
    font-size: var(--dnd-text-xs);
    line-height: 1.45;
  }

  .dnd-newsletter-dialog__consent input {
    flex: 0 0 auto;
    margin-top: 3px;
  }

  .dnd-newsletter-dialog__form > button {
    min-height: 48px;
    margin-top: 4px;
    border: 1px solid #d99e2b;
    border-radius: 5px;
    background: #d99e2b;
    color: #201913;
    cursor: pointer;
    font-size: var(--dnd-text-md);
    font-weight: 600;
  }

  .dnd-newsletter-dialog__form > button:hover,
  .dnd-newsletter-dialog__form > button:focus-visible {
    background: #f1bd54;
  }

  .dnd-newsletter-dialog__response {
    width: 100%;
    height: 320px;
    margin-top: 18px;
    border: 1px solid rgba(91, 70, 42, 0.14);
    border-radius: 8px;
    background: #fff;
  }

  @media (max-width: 960px) {
    .dnd-site-footer {
      padding: 44px 0 20px;
    }

    .dnd-site-footer__inner {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .dnd-site-footer__brand {
      max-width: none;
      grid-column: 1 / -1;
    }

    .dnd-site-footer__group--stays {
      grid-column: 1 / -1;
    }
  }

  @media (max-width: 720px) {
    .dnd-site-footer {
      margin-top: 24px;
      padding: 40px 0 18px;
    }

    .dnd-site-footer__inner {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 22px;
    }

    .dnd-site-footer__brand {
      grid-column: 1 / -1;
      text-align: center;
      justify-self: center;
      border-left: 0;
      border-radius: 16px;
      background: radial-gradient(circle at 50% 0, rgba(249, 203, 91, 0.09), transparent 62%);
    }

    .dnd-site-footer::before {
      top: 92px;
      right: -96px;
      width: 240px;
      height: 210px;
      opacity: 0.46;
    }

    .dnd-site-footer__group h2 {
      margin-bottom: 10px;
    }

    .dnd-site-footer__stay-columns {
      gap: 12px 18px;
    }

    .dnd-site-footer__group li + li {
      margin-top: 8px;
    }

    .dnd-site-footer__group a {
      font-size: var(--dnd-text-md);
    }

    .dnd-site-footer__bottom {
      margin-top: 26px;
      padding-top: 14px;
    }

    .dnd-site-footer__newsletter {
      align-items: stretch;
      flex-direction: column;
      gap: 12px;
      margin-bottom: 28px;
      text-align: center;
    }

    .dnd-site-footer__newsletter-form {
      width: 100%;
    }
  }
</style>

<script>
  const newsletterDialog = document.querySelector("[data-newsletter-dialog]");
  const newsletterEmail = newsletterDialog?.querySelector("[data-newsletter-email]");
  const newsletterForm = newsletterDialog?.querySelector("[data-newsletter-submit]");
  const newsletterSuccess = newsletterDialog?.querySelector("[data-newsletter-success]");
  const newsletterResponse = newsletterDialog?.querySelector("[data-newsletter-response]");

  if (
    newsletterDialog instanceof HTMLDialogElement &&
    newsletterEmail instanceof HTMLInputElement &&
    newsletterForm instanceof HTMLFormElement &&
    newsletterSuccess instanceof HTMLElement
  ) {
    document.querySelectorAll("[data-newsletter-trigger]").forEach((trigger) => {
      trigger.addEventListener("submit", (event) => {
        event.preventDefault();

        const emailInput = trigger.querySelector('input[name="email"]');
        if (!(emailInput instanceof HTMLInputElement) || !emailInput.value.trim()) {
          return;
        }

        newsletterForm.hidden = false;
        newsletterSuccess.hidden = true;
        newsletterForm.reset();
        newsletterEmail.value = emailInput.value.trim();
        if (newsletterResponse instanceof HTMLIFrameElement) {
          newsletterResponse.hidden = true;
          newsletterResponse.src = "about:blank";
        }
        newsletterDialog.showModal();
        newsletterEmail.focus();
      });
    });

    newsletterDialog.querySelectorAll("[data-newsletter-close]").forEach((closeButton) => {
      closeButton.addEventListener("click", () => newsletterDialog.close());
    });

    newsletterDialog.addEventListener("click", (event) => {
      if (event.target === newsletterDialog) {
        newsletterDialog.close();
      }
    });

    newsletterForm.addEventListener("submit", () => {
      newsletterForm.hidden = true;
      newsletterSuccess.hidden = false;
      newsletterSuccess.focus();
      if (newsletterResponse instanceof HTMLIFrameElement) {
        newsletterResponse.hidden = true;
      }
    });
  }
</script>
